class Scene {
  constructor(stage, renderer) {
    this.stage = stage;
    this.renderer = renderer;
  }

  // Speed (per millisecond)
  // return List of in Tweens
  in() {

  }

  // Speed (per millisecond)
  // return List of out Tweens
  out() {

  }
}

export default Scene;
